# Project Agent Rules

Read `DESIGN.md` and `AGENTS.md` before changing this project. Follow the shared precedence and portfolio constraints in `AGENTS.md`.

Use `.claude/skills/ponytail/SKILL.md` at `full` intensity for coding. The complete Ponytail suite is installed beside it.

The user selected Anti-Slop `DURING` mode for this project on 2026-09-18. Do not ask again unless the user explicitly requests an `AFTER` audit. Load `.claude/skills/antislop/SKILL.md` plus the matching `.claude/skills/antislop-*` skills for UI, copy, accessibility, responsive layout, and code comments.

Run the relevant Anti-Slop Delivery Gate before finishing. Preserve the existing portfolio identity and use Ponytail to produce the smallest correct implementation.
